# PredictOps — pinned to the versions the reported results were produced with.
# Python 3.11+ (developed and measured on 3.14.4). CPU only; no GPU required.

# --- core numerics -------------------------------------------------------
numpy==2.4.6
pandas==3.0.3
scipy==1.17.1
pyarrow==25.0.1

# --- models ---------------------------------------------------------------
scikit-learn==1.9.0
xgboost==3.4.1
lightgbm==4.7.0
# CPU build. On Linux/Windows install from the CPU index if the default wheel
# pulls CUDA:  pip install torch==2.12.0 --index-url https://download.pytorch.org/whl/cpu
torch==2.12.0

# --- service --------------------------------------------------------------
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.4

# --- tests ----------------------------------------------------------------
pytest==9.1.1

# --- optional LLM providers ----------------------------------------------
# Not required: the pipeline runs end to end on MockProvider with no API key,
# and no measured metric depends on an LLM.
# anthropic>=0.40
# openai>=1.50
